package OopConceptInterfacePart2;

public interface USBank {
	int min_bal=200;
	
	public void credit();
	
	public void debit();
	
	public void transmoney();
	
     //only method declaration
	//no method boday - only method prototype
	//in interface we can declare the variable--var are by default static in nature
	//variable value is not be changed -its final
	//no static method in interface
	//no main method in interface
	//we can not create object in interface
	//interface in abstract in interface
	
	
	
	
}
